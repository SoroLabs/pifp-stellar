import React from 'react';

export const LiquidityHeatmap: React.FC = () => {
    return (
        <div>
            <h2>Liquidity Heatmap</h2>
            <canvas id="heatmap-canvas"></canvas>
        </div>
    );
};
