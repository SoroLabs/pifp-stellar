'use client'

/**
 * LedgerHeartbeat is now coordinated centrally via AppContext.
 * This component remains as a clean pass-through for architectural backwards compatibility.
 */
export default function LedgerHeartbeat() {
  return null
}
